name: Windows QT Build & Release

on:
 push:
   tags:
     - "v*.*.*"

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
    - name: Clone repository
      uses: actions/checkout@v4

    - name: Install pkg-config
      run: choco install pkgconfiglite 7zip.portable -y

    - name: Install Qt and build tools
      uses: jurplel/install-qt-action@v4
      with:
        aqtversion: '==3.1.*'
        version: '6.8.3'
        host: 'windows'
        target: 'desktop'
        arch: 'win64_mingw'
        modules: 'qtshadertools'
        tools: 'tools_mingw1310'
        cache: true

    - name: Configure CMake
      run: |
        mkdir build
        cd build
        cmake .. -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release
    
    - name: Build
      run: |
        cd build
        mingw32-make
    
    - name: Run windeployqt
      run: |
        mkdir rel
        cp build/sang-box.exe rel/sang-box.exe
        cd rel
        windeployqt sang-box.exe --qmldir ..
    
    - name: Create sang-box.zip
      run: 7z a -tzip sang-box-${{ github.ref_name }}.zip rel
    
    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: sang-box-${{ github.ref_name }}
        path: sang-box-${{ github.ref_name }}.zip

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v2
      if: github.ref_type == 'tag'
      with:
        files: |
          sang-box-${{ github.ref_name }}.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}